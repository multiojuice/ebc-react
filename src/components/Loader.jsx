import React from 'react';
import background from '../website_assets/looping_smallest.gif'

export default () => {
    return (
        <header className="full-section" style={{ background: `url(${background}) no-repeat center center fixed`, backgroundSize: 'cover'}}>

        </header>
    )
}
